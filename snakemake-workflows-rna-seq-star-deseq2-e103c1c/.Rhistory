library(biomaRt)
# example: connect to Ensembl
mart <- useEnsembl(biomart="genes", dataset="hsapiens_gene_ensembl")
# try mapping a gene ID
getBM(attributes=c("ensembl_gene_id", "hgnc_symbol"), 
      filters="ensembl_gene_id", 
      values=c("ENSG00000139618"), 
      mart=mart)
quit()
